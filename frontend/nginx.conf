server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Esta es la línea mágica para que React Router funcione al recargar
        try_files $uri $uri/ /index.html;
    }
}
